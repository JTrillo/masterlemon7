{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "tsc --noEmit && rimraf dist && parcel index.html",
    "build:prod": "tsc --noEmit && rimraf dist && parcel build index.html",
    "start": "tsc --noEmit && rimraf dist && parcel index.html --open"
  },
  "keywords": [],
  "author": "Joaquín Trillo Escribano",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.9.0",
    "@babel/plugin-transform-runtime": "^7.9.0",
    "@babel/runtime": "^7.9.2",
    "parcel": "^1.12.4",
    "rimraf": "^3.0.2",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "graphql-request": "^1.8.2"
  }
}
