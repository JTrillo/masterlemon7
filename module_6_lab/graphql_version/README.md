# Module 6 Optional Exercise

`client` folder contains a client that uses an API implemented with GraphQL. To start it:
```bash
npm start
```

`server` folder contains API Car using GraphQL. To start it:
```bash
npm start
```