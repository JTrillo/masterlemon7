import React from 'react';

export const LechComponent: React.FunctionComponent = () => {
    const stadium = "INEA Stadion";

    return (
        <div>
            <span>We can see the Lech Poznań badge and its stadium: {stadium}</span>
        </div>
    );
};