const prefix = "mimodulo"; //para evitar colisiones

export const actionsIds = {
  //UPDATE_USERPROFILE_NAME: `${prefix}/miredux/update-user-profile-name`
  UPDATE_USERPROFILE_NAME: "/miredux/update-user-profile-name",
  MEMBER_REQUEST_COMPLETED: "/miredux/member-request-completed"
};